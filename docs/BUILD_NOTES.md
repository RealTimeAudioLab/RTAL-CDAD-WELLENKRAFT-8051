# Build Notes

This archive contains the original Assembly sources and the original HEX/BIN outputs.

## Original Toolchain

The exact original toolchain configuration should be documented here.

Known target:

- Silicon Labs C8051F120
- 8051 Assembly source files (`.a51`)
- Include files (`.inc`)
- HEX/BIN output for programming

## Recommended Next Steps

Before publishing the repository, add:

1. original assembler / IDE name,
2. exact project settings,
3. memory model,
4. linker configuration,
5. flash programming tool,
6. required include path order,
7. instructions for building Module 1,
8. instructions for building Module 2,
9. programming procedure for each C8051F120.

## Reproducibility

The included HEX/BIN files provide a known historical build output.  
A future goal should be to make the build fully reproducible from the source files.
